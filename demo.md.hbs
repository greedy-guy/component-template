---
title: Simple Usage
---

本 Demo 演示一行文字的用法。

```jsx
import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import {{upperCamelCased pkgName}} from '{{pkgScope}}/{{pkgName}}';

class App extends Component {
  render() {
    return (
      <div>
        <{{upperCamelCased pkgName}} />
      </div>
    );
  }
}

ReactDOM.render((
  <App />
), mountNode);
```
